# Node
node_modules/
.next/
*.tsbuildinfo

# Local env
.env
.env.local
!.env.example

# Runtime data (Railway Volume)
data/

# Stray plugin / CI files — this branch is web-only, the plugin lives on master
paper-plugin/
.github/

# Transfer-bundle artifacts — session docs + a prod snapshot that
# carries data.db + config.yml secrets. Must never land in git.
backup-*/
HANDOFF_*.md
taste-skill/

# Experimental UI prototypes — login redesign, mcrender-style studio,
# avatar generator. Kept local for future work but excluded from the
# prod build (App Router pages with WIP type errors that break
# `next build`). Swept in accidentally by a `git add -A` in 1.0.13.
src/app/login-preview/
src/app/studio/
src/app/p/
src/app/avagen/
src/app/api/skin/
player.gltf
public/avagen/
public/hats/
public/clouds.png
public/mc-tex/
public/models/
public/textures/
public/login-assets/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml
