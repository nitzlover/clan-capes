# Node
node_modules/
.next/
*.tsbuildinfo

# Local env
.env
.env.local
!.env.example

# Runtime data (Railway Volume)
data/

# Stray plugin / CI files — this branch is web-only, the plugin lives on master
paper-plugin/
.github/

# Transfer-bundle artifacts — session docs + a prod snapshot that
# carries data.db + config.yml secrets. Must never land in git.
backup-*/
HANDOFF_*.md
taste-skill/

# Avatar-generator prototype — still WIP, excluded from the prod build.
src/app/avagen/
player.gltf
public/avagen/
public/mc-tex/
public/models/
public/textures/
public/login-assets/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml
